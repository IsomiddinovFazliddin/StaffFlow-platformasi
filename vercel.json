{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "cd staffflow && npm install && npm run build",
  "outputDirectory": "staffflow/dist",
  "installCommand": "cd staffflow && npm install"
}
